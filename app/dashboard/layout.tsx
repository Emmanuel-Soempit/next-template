import { ReactNode } from "react";

function DashboardMainLayout({ children }: { children: ReactNode }) {
  return (
    <div>
      {/* Your dasboard layout goes here */}
      {children}
    </div>
  );
}

export default DashboardMainLayout;
